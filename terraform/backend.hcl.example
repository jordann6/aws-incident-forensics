# Copy to backend.hcl (gitignored) and fill in your values, then:
#   terraform init -backend-config=backend.hcl
bucket       = "your-terraform-state-bucket"
key          = "aws-incident-forensics/terraform.tfstate"
region       = "us-east-1"
use_lockfile = true
